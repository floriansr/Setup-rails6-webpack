<% if @current_user_can_see_image %>
  <%= render partial: "one_image", locals: {image: @image}%>
  <%= render partial: "comments/new" %>
<% else %>
  <p>This image is private !</p>
<% end %>

<%= link_to 'Back to Gallery', images_path %>